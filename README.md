# Summary

Created for a school assignment

Given a list of urls in a passed text file, attempts to connect to each url via a TCP socket and fetch data from them.
Built and tested on Linux, will NOT run on Windows.

Uses OpenSSL 3.5.5 to manage HTTPS connections

# Instructions

To build: `make` ; To run: `./monitor <path_to_urls_file>`
